#include <Eigen/Dense>
using namespace Eigen;

class Source {
	
	/*Vector3f location = Vector3f().Zero();

	public float amount = 0;

	public Source(Vector3d p, float a) {
		location = p;
		amount = a;
	}*/
};